export interface NavigationItem {
  label: string;
  href: string;
}

export interface HeroContent {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
}

export interface StudioInfo {
  name: string;
  logo: string;
  tagline: string;
}
